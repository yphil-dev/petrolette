{
  "name": "petrolette",
  "description": "The news page that doesn't know you",
  "version": "1.8.0",
  "private": true,
  "license": "GPL-3.0-only",
  "engines": {
    "node": "*"
  },
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/yphil/petrolette"
  },
  "scripts": {
    "build:edge": "node ./edge/build-assets.js",
    "dev:edge": "npm run build:edge && npx wrangler dev --config edge/wrangler.jsonc",
    "deploy:edge": "npm run build:edge && npx wrangler deploy --config edge/wrangler.jsonc",
    "test": "npm run build:edge && npx wrangler deploy --config edge/wrangler.jsonc --dry-run",
    "test:feeds": "mocha --timeout 600s test/defaultFeeds.test.js"
  },
  "dependencies": {
    "@fancyapps/fancybox": "3.5.7",
    "cheerio": "^1.2.0",
    "dompurify": "^3.3.1",
    "feedparser": "^2.2.10",
    "intro.js": "^7.2.0",
    "jquery": "^3.6.3",
    "jquery-ui-dist": "^1.13.3",
    "mousetrap": "1.6.5",
    "remotestorage-widget": "^1.8.0",
    "remotestoragejs": "^2.0.0-beta.8",
    "responsively-lazy": "^2.0.2"
  },
  "devDependencies": {
    "mocha": "^10.8.2",
    "should": "^13.2.3"
  }
}
